import type { NextConfig } from "next";

const nextConfig = {
  output: "export", // 👈 This replaces the need for 'next export'
  // (optional) Add other config options below
};

export default nextConfig;
