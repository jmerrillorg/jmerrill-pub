# Copyediting Alignment
CONFLICT — same root cause as Line: no COPYEDITING branch in buildOutputDocument(), identical generic boilerplate produced, no actual mechanical correction occurs. No before/after diff/similarity guard exists anywhere to prevent non-mechanical rewriting, though this is currently moot since no real copyedit output exists yet. See remediation item 1 (P0).
