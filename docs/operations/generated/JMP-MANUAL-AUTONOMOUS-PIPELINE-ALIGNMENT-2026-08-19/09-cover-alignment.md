# Cover Intelligence Alignment
Concept vs Full-Wrap distinction: PARTIAL — the type exists (publisher-operating-center.ts coverReadiness: FRONT COVER APPROVED / FULL WRAP APPROVED are distinct literals) and is used in some reporting paths, but the function that actually computes cover readiness (productionReadinessFromWorkload) never emits these specific values, and the real distribution gate (milestone8DistributionSetupReadiness.js) collapses everything to a flat COVER_APPROVED boolean. The Intentional Leader's correct concept-only classification this session was preserved through manual ops documentation, not this runtime distinction — confirming the gap is real.
Distribution-path awareness: MISSING — trim/spine/page-count are opaque checklist labels with no calculation or per-title template logic.
Ingram hardcoding: CONFLICT — distributionRail hardcoded to INGRAM_CONTENT for every title in productionPipelineV2Doctrine.js; zero Lightning Source references anywhere in the codebase.
