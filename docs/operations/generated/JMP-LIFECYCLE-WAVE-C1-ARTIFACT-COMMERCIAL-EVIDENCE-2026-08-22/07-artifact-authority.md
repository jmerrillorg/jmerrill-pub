# Artifact Authority

Artifact identity/type/title binding remain complete in production readback. Provenance remains high but not universal. Checksum remains 0/314 because no governed SHA-256 field or external hash was surfaced in the production asset rows.

C.1 rule: checksum support requires explicit checksum or read-only byte source plus separate additive evidence-write authorization. A path, SharePoint URL, title, version, or artifact class cannot fabricate checksum authority.
