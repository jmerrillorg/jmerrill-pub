# Workspace Entitlement

Workspace entitlement is now a separate evidence facet. It is SUPPORTED only when workspaceEntitlementState is explicitly surfaced to the projection; otherwise it remains STRUCTURAL DATA_GAP.
