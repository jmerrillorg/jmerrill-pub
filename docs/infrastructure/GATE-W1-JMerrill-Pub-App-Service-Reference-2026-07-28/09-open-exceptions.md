# Open Exceptions

| Exception ID | Description | Risk | Production Impact | Recommended Remediation | Blocking |
| --- | --- | --- | --- | --- | --- |
| GATE-W1-EX-004 | Dependency-specific monitoring is not fully implemented. | Incidents in Dataverse, Graph, ACS, storage, or diagnostic runner may be detected later than desired. | App Service metrics/logs are operational; dependency alerting remains immature. | Add dependency-specific synthetic availability and scheduled-query alerts. | No |
| GATE-W1-EX-005 | Local DNS/HTTP stacks may cache the former SWA target until TTL expiry. | Temporary operator confusion during propagation. | Public resolvers show App Service targets; canonical host health passed with App Service IP. | Recheck after TTL and retain the pre-cutover rollback snapshot. | No |
| GATE-W1-EX-007 | Final positive `/join` replay is incomplete on a workflow-deployed authoritative staging release. The `JMP-INT-202607-YEUSKK` proof remains valid progress, but it is tied to the earlier `a3a006bcf8839326f4270e789c1697c0d1ad68b7` release rather than a default-branch workflow deployment. | Business-path evidence is not yet bound to the final governed release authority. | No production data or payment behavior changed, but reference certification still lacks final release-bound intake evidence. | After the App Service workflow deploys staging, replay the governed synthetic `/join` matrix with valid Turnstile, nonduplicative Dataverse proof, duplicate retry, and invalid request controls. | Yes |
| GATE-W1-EX-008 | Fresh governed synthetic Author Operating Center own-artifact proof is incomplete. Synthetic gate/context, no-cookie denial, forged-session denial, cross/missing artifact 404, logout, and post-logout denial passed for `JMP-INT-202607-YEUSKK`, but the synthetic project has no delivered author-facing artifact backed by Graph content, so own-artifact 200, filename, MIME type, and checksum cannot be proven honestly. | Full current-window AOC artifact evidence is incomplete. | No production access was weakened; the author context and denial controls passed, but own artifact download remains unproven. | Create a governed staging-only author-facing editorial artifact with backing Graph file for the synthetic asset, prove own download 200 and cross-author 404, then archive/retire the fixture per evidence policy. | Yes |

## Exception Decision

Because GATE-W1-EX-007 and GATE-W1-EX-008 are blocking for an enterprise reference pattern, the certification decision remains NOT_CERTIFIED. App Service workflow deployment, staging auto-swap control, and staging-only immutable-artifact rollback/roll-forward are now proven on `172779c04df6d7e7adf6ee1fad96664cbbf2ac61`.
