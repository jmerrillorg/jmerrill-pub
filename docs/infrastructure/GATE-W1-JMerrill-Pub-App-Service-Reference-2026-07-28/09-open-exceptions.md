# Open Exceptions

| Exception ID | Description | Risk | Production Impact | Recommended Remediation | Blocking |
| --- | --- | --- | --- | --- | --- |
| GATE-W1-EX-001 | Positive production /join submission was not completed because Turnstile still reported client error 110200 on the App Service staging hostname. | A critical business path is not independently proven on App Service. | Authors may still be able to submit manually on authorized production hostnames, but reference certification lacks positive staging evidence. | Add `app-jm1-pub-prod-staging.azurewebsites.net` to the existing Publishing `/join` Turnstile widget Hostname Management, then complete a token-bearing synthetic submission without bypassing Turnstile. | Yes |
| GATE-W1-EX-003 | Slot-swap and rollback proof is incomplete. Staging auto-swap was found enabled and was disabled during this pass. | Enterprise reference pattern lacks repeatable rollback evidence. | DNS rollback values are preserved and SWA remains available, but swap rollback was not proven. | Complete a no-drift slot deployment and swap/rollback exercise after staging stability is proven, or formally approve DNS rollback as the GATE-W1 rollback control. | Yes |
| GATE-W1-EX-004 | Dependency-specific monitoring is not fully implemented. | Incidents in Dataverse, Graph, ACS, storage, or diagnostic runner may be detected later than desired. | App Service metrics/logs are operational; dependency alerting remains immature. | Add dependency-specific synthetic availability and scheduled-query alerts. | No |
| GATE-W1-EX-005 | Local DNS/HTTP stacks may cache the former SWA target until TTL expiry. | Temporary operator confusion during propagation. | Public resolvers show App Service targets; canonical host health passed with App Service IP. | Recheck after TTL and retain the pre-cutover rollback snapshot. | No |
| GATE-W1-EX-006 | App Service GitHub Actions release pipeline is not implemented or proven; the existing workflow still targets Azure Static Web Apps. | The reference implementation does not yet prove repeatable App Service deployment through CI/CD. | Production currently operates on App Service, but release repeatability depends on manual package deployment. | Add and validate a Publishing-only App Service deployment workflow with governed secrets/OIDC and an explicit staging-to-production promotion model. | Yes |
| GATE-W1-EX-007 | App Service staging health is unstable after deployment/restart. The slot recovered to 200 after warmup, but restart-adjacent `/api/health` probes timed out after 45 seconds. | Staging cannot yet serve as a reliable promotion or rollback target. | Production remained healthy, but reference certification cannot rely on staging. | Diagnose staging startup/runtime stability, including package shape, startup command, health probe timing, instance warmup, and Key Vault reference resolution under restart. | Yes |
| GATE-W1-EX-008 | Fresh own-artifact download proof was not repeated after the prior synthetic Dataverse and SharePoint fixture was retired. Native gate/session issuance and denial/logout controls passed. | Full current-window AOC artifact evidence is incomplete. | No production access was weakened; prior evidence proved isolation, but the current certification package lacks a fresh own-artifact positive proof. | Recreate a governed certification-only fixture with one synthetic author-owned artifact and one cross-author artifact, repeat own-artifact 200 and cross-author 404, then retire/archive fixture after evidence capture. | Yes |

## Exception Decision

Because GATE-W1-EX-001, GATE-W1-EX-003, GATE-W1-EX-006, GATE-W1-EX-007, and GATE-W1-EX-008 are blocking for an enterprise reference pattern, the certification decision is NOT_CERTIFIED.
