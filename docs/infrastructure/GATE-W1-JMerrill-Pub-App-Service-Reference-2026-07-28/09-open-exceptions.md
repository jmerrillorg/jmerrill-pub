# Open Exceptions

| Exception ID | Description | Risk | Production Impact | Recommended Remediation | Blocking |
| --- | --- | --- | --- | --- | --- |
| GATE-W1-EX-001 | Positive production /join submission was not completed. A restricted Cloudflare Turnstile widget was created because no existing widget was visible; it is limited to `jmerrill.pub`, `www.jmerrill.pub`, and `app-jm1-pub-prod-staging.azurewebsites.net`. Key Vault Turnstile secrets were rotated, staging config returned a valid site-key presence signal, and the widget rendered on staging, but Cloudflare returned `failure_retry` in the controlled desktop browser session and did not issue a token. | A critical business path is not independently proven on App Service. | Authors may still be able to submit manually on authorized production hostnames, but reference certification lacks positive staging evidence. | Complete a token-bearing synthetic submission without bypassing Turnstile from a browser context accepted by Cloudflare, then prove 201 intake receipt, one Dataverse intake, one manuscript, orchestration dispatch, recommendation path, and no duplicate durable work. | Yes |
| GATE-W1-EX-003 | Slot-swap and rollback proof is incomplete. Staging auto-swap was found enabled and was disabled during this pass. | Enterprise reference pattern lacks repeatable rollback evidence. | DNS rollback values are preserved and SWA remains available, but swap rollback was not proven. | Complete a no-drift slot deployment and swap/rollback exercise after staging stability is proven, or formally approve DNS rollback as the GATE-W1 rollback control. | Yes |
| GATE-W1-EX-004 | Dependency-specific monitoring is not fully implemented. | Incidents in Dataverse, Graph, ACS, storage, or diagnostic runner may be detected later than desired. | App Service metrics/logs are operational; dependency alerting remains immature. | Add dependency-specific synthetic availability and scheduled-query alerts. | No |
| GATE-W1-EX-005 | Local DNS/HTTP stacks may cache the former SWA target until TTL expiry. | Temporary operator confusion during propagation. | Public resolvers show App Service targets; canonical host health passed with App Service IP. | Recheck after TTL and retain the pre-cutover rollback snapshot. | No |
| GATE-W1-EX-006 | App Service GitHub Actions release pipeline is source-present but not yet proven. The existing successful check still targets Azure Static Web Apps; the new Publishing App Service workflow has not completed a governed run. | The reference implementation does not yet prove repeatable App Service deployment through CI/CD. | Production currently operates on App Service, but release repeatability still depends on manual package deployment until the new workflow is exercised. | Run and validate the Publishing App Service workflow with OIDC, immutable artifact retention, staging health proof, approval-gated production promotion, and rollback observation. | Yes |
| GATE-W1-EX-007 | App Service staging health is unstable after deployment/restart. The latest restart reproduced fast stale-handler responses, multiple 30-second health timeouts, and then recovery to the new handler with route duration 1 ms. | Staging cannot yet serve as a reliable promotion or rollback target. | Production remained healthy, but reference certification cannot rely on staging. | Continue diagnosing cold-start/process activation behavior, SCM restart overlap, package startup, health probe timing, and App Service plan/resource behavior. | Yes |
| GATE-W1-EX-008 | Fresh governed synthetic Author Operating Center fixture proof is incomplete. The active registry has no synthetic fixture marker; master-code issuance, cookie setting, context, logout, and post-logout denial passed. | Full current-window AOC artifact evidence is incomplete. | No production access was weakened; prior evidence proved isolation, but the current certification package lacks a fresh own-artifact positive proof. | Recreate a governed certification-only fixture with one synthetic author-owned artifact and one cross-author artifact, add a temporary staging-only synthetic grant, repeat own-artifact 200 and cross-author 404, then retire/archive fixture after evidence capture. | Yes |

## Exception Decision

Because GATE-W1-EX-001, GATE-W1-EX-003, GATE-W1-EX-006, GATE-W1-EX-007, and GATE-W1-EX-008 are blocking for an enterprise reference pattern, the certification decision remains NOT_CERTIFIED.
