# Open Exceptions

| Exception ID | Description | Risk | Production Impact | Recommended Remediation | Blocking |
| --- | --- | --- | --- | --- | --- |
| GATE-W1-EX-001 | Positive production /join submission was not completed because Turnstile did not issue a token in automated browser validation. | A critical business path is not independently proven on App Service. | Authors may still be able to submit manually, but reference certification lacks positive evidence. | Use an approved human-assisted Turnstile validation or governed non-production Turnstile testing method that does not weaken production controls. | Yes |
| GATE-W1-EX-002 | App Service staging /api/author/gate rejected temporary certification-only synthetic access settings with 401 after the parser compatibility fix was deployed to staging. | Native staging session issuance path remains unproven, although denial is fail-closed. | No production access was weakened; staging fixture certification incomplete. | Diagnose why staging runtime does not accept temporary synthetic access settings, or provision an approved staging fixture through the governed Key Vault-backed registry and repeat the AOC matrix. | Yes |
| GATE-W1-EX-003 | Slot-swap and rollback proof is incomplete. Staging auto-swap was found enabled and was disabled during this pass. | Enterprise reference pattern lacks repeatable rollback evidence. | DNS rollback values are preserved and SWA remains available, but swap rollback was not proven. | Complete a no-drift slot deployment and swap/rollback exercise after staging stability is proven, or formally approve DNS rollback as the GATE-W1 rollback control. | Yes |
| GATE-W1-EX-004 | Dependency-specific monitoring is not fully implemented. | Incidents in Dataverse, Graph, ACS, storage, or diagnostic runner may be detected later than desired. | App Service metrics/logs are operational; dependency alerting remains immature. | Add dependency-specific synthetic availability and scheduled-query alerts. | No |
| GATE-W1-EX-005 | Local DNS/HTTP stacks may cache the former SWA target until TTL expiry. | Temporary operator confusion during propagation. | Public resolvers show App Service targets; canonical host health passed with App Service IP. | Recheck after TTL and retain the pre-cutover rollback snapshot. | No |
| GATE-W1-EX-006 | App Service GitHub Actions release pipeline is not implemented or proven; the existing workflow still targets Azure Static Web Apps. | The reference implementation does not yet prove repeatable App Service deployment through CI/CD. | Production currently operates on App Service, but release repeatability depends on manual package deployment. | Add and validate a Publishing-only App Service deployment workflow with governed secrets/OIDC and an explicit staging-to-production promotion model. | Yes |
| GATE-W1-EX-007 | App Service staging health is unstable after restore. The slot initially returned 200 after standalone redeployment and Key Vault restore, but final `/api/health` probes timed out after 20 seconds and 60 seconds. | Staging cannot serve as a reliable promotion or rollback target. | Production remained healthy, but reference certification cannot rely on staging. | Diagnose staging startup/runtime stability, including package shape, Oryx startup behavior, health probe timing, and Key Vault reference resolution under restart. | Yes |

## Exception Decision

Because GATE-W1-EX-001, GATE-W1-EX-002, GATE-W1-EX-003, GATE-W1-EX-006, and GATE-W1-EX-007 are blocking for an enterprise reference pattern, the certification decision is NOT_CERTIFIED.
